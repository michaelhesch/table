$(document).ready(function() {
    $("h2").addClass("underline"); //adds underline classs to all h2 elements once DOM has loaded
    $("ul").addClass("border"); //adds border class to all list elements once DOM has loaded
});